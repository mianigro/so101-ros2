Revise ICL_IMPLEMENTATION.md to reflect verification findings and the user's two-stage strategy (foundational pi05 → ICL pre-train on open-source data → fine-tune with ICL on own robot data). Document edits only; no code, no downloads.

## 1. Base-model switch (§1 table, §2, §4.1, §4.3, §7)
- Replace `base_checkpoint: self-improve/rounds/round_N/best` with `lerobot/pi05_base` (foundational, openpi-converted, cached at ~/.cache/huggingface/hub/models--lerobot--pi05_base; fp32 safetensors → note bf16 load requirement).
- Remove all references to the SO-101 fine-tune as the frozen base; note no such checkpoint exists on disk and full π0.5 fine-tuning needs ≥40 GB VRAM (self-improve README), so foundation+adapters is the only hardware-feasible configuration on 2×4080.
- M0 zero-init/parity gate now compares against pi05_base outputs.

## 2. Correct the false/inaccurate §1 integration rows + §10
- policy_server row: NOT zero edits — `inference_engine.py:33-43` hardcodes SUPPORTED_POLICIES and `load_policy` (:290-292) raises before `get_policy_class`. One-line addition of "pi05_icl" (precedent: xvla). Fix §10's "no changes to policy_server" claim accordingly.
- so101_30hz.yaml row: correct it — no camera features in that yaml (cameras injected from --setup via so101_bringup/config/setups), chunk_size/max dims are pi05_base policy-config values, datasets are AV1 video not JPEG.
- Fix `_build_peft_config` anchor to pretrained.py:493; describe the registry precisely (draccus choice registry, convention-based: `<X>Config` in `configuration_*` → `<X>Policy` in `modeling_*`; must also provide `processor_pi05_icl.make_pi05_icl_pre_post_processors` delegating to pi05's, or processor creation raises; subclasses must define `config_class` and `name`).
- Add row/note: demo-frame preprocessing reuses `PI05Policy._preprocess_images` (:952-1016), where the resize-with-pad + [-1,1] SigLIP transform actually lives.

## 3. Add critical engineering gotchas section
- `PI05Policy.from_pretrained` defaults `strict=True` and swallows load failures with only a printed warning — ICL loader MUST pass `strict=False` (demo_encoder/gate keys legitimately missing from base checkpoint); M0 test must assert base weights actually loaded (e.g., compare a frozen param against checkpoint).
- `wrap_with_peft` freezes ALL params before wrapping (would freeze demo_encoder) — confirm the manual `inject_adapter_in_model` approach; note the default pi05 peft regex targets non-existent modules (`state_proj`, `action_time_mlp_*`) so overriding targets is mandatory.
- pi05 has no `state_proj`: state is tokenized into the PaliGemma prompt; `max_state_dim=32` is prompt padding, not a projection.
- extend `embs`/bool `pad_masks`/`att_masks` (0-entries) consistently; cast demo embeddings to the prefix dtype (bf16 when expert is bf16), width 2048.

## 4. Restructure for two-stage training (§4.4 data.py, §6, §7, §8)
- **Stage 1 — ICL pre-training on open-source data**: `lerobot/droid_1.0.1` primary (LeRobot v3.0 native in 0.6.1; 95,658 episodes; group support/query by `task_category` column → 86 tasks × ~1,100 episodes — NOT the 49,630 raw task strings; cameras exterior_1_left/exterior_2_left/wrist_left renamed to pi05_base openpi names base_0_rgb/left_wrist_0_rgb/right_wrist_0_rgb; state/action 8-dim padded to 32; Apache-2.0; ~810 GB full → chunk-subset download strategy: v3.0 chunks = 1,000 episodes / ~8.5 GB, sampler must build task_registry from downloaded chunks only since global meta covers all episodes; `lerobot/droid_100` 464 MB as smoke test). Fallback documented: `nvidia/BridgeData2_LeRobot_v3` (90 GB, needs env+skill grouping, WidowX).
- **Stage 2 — fine-tune with ICL on own SO-101 data**: continue training the SAME LoRA + DemoEncoder + gates at lower LR (~1-3k steps) on rosbag_to_lerobot exports; camera rename via existing `PI05_BASE_IMAGE_KEY_MAP` (self-improve/contract.py:75-79); per-stage quantile normalization stats (pre/post processors rebuilt from the stage's dataset).
- Note embodiment-gap handling (Franka 8-dim/15 fps → SO-101 6-dim/30 fps: 32-dim padding + prompt-tokenized state; stage-2 continued training adapts).
- Note current own-data reality (anchor ≈ 1 task × 4 episodes; §7's local/so101_teleop_v1 and local/so101_sim_round2 don't exist) — stage 2 depends on data collection/quality gates as the doc already targets.

## 5. Config schema (§7)
- Split into `icl_pretrain_droid_v1.yaml` (stage 1: base lerobot/pi05_base, droid subset, task_category registry, holdout task_categories) and `icl_finetune_so101_v1.yaml` (stage 2: `init_adapter_from:` stage-1 artifact, own datasets, lower LR, fewer steps).

## 6. Milestones (§8)
- M0 unchanged in substance but vs pi05_base + strict=False/weights-actually-loaded assertion.
- M1 sampler overfit on droid_100.
- M2 = stage-1 gate: held-out DROID task_categories, demo-conditioned query loss clearly below demo-zeroed loss, no drift on seen categories.
- M3 = stage-2 robot fine-tune + sim/real eval gate (original M2's success-rate criteria).
- M4/M5 = serve_icl real-robot eval and bridge_icl_node (original M3/M4 shifted).

## 7. Risks (§9) additions
- Silent strict-load failure (mitigated by M0 assertion).
- Embodiment gap stage 1→2 (mitigation: continued adapter training; optional expert-LoRA knob).
- DROID disk/subset constraints and sampler-restricted-to-downloaded-chunks.
- Stage-1/stage-2 processor-stats skew (single transform builder; stats sourced from active policy preprocessor).
- DROID task-string noise → task_category grouping.