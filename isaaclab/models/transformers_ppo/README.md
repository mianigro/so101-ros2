# To Do
- Add in a skip connection for final layer, similar to resnet, transformer, etc

# Start
## Re-build From Scratch
`docker compose build --no-cache`


## Rebuild Layers and Run
`docker compose up --build -d`


## Run
`docker compose up -d`